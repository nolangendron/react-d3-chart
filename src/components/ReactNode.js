import React from 'react';

const styles = {
  height: '100%',
  backgroundColor: 'red'
}

const ReactNode = ({ dims }) => {
  return (
    <div style={styles}>hello</div>
  )
};

export default ReactNode;